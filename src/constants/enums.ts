export enum FRAME_MODE {
  HORIZONTAL = "horizontal",
  VERTICAL = "vertical",
}

export enum TYPE_FRAME {
  MATTED_FRAME = "Matted Frame",
  REGULAR_FRAME = "Regular Frame",
  FLOATING_FRAME = "Floating Frame",
}

export enum IMG_TYPE {
  JPG = "jpg",
  PNG = "png",
}

export enum FRAME_SIZES {
  MAX_CONTAINER_SIZE = 1600,
  HEADER_HEIGHT = 60,
  MARGINS = 50,
}

export enum LANGUAGE {
  pl = "pl",
  en = "en",
}
